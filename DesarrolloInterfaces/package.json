{
    "name": "desarrollo-interfaces-root",
    "private": true,
    "scripts": {
        "install": "cd ServerMonitorApp/frontend && npm install",
        "dev": "cd ServerMonitorApp/frontend && npm run dev",
        "build": "rm -rf dist && mkdir dist && cp index.html dist/ && cd ServerMonitorApp/frontend && npm install && npm run build && cd ../.. && mkdir -p dist/ServerMonitorApp/frontend && cp -r ServerMonitorApp/frontend/out/* dist/ServerMonitorApp/frontend/"
    },
    "engines": {
        "node": ">=18"
    }
}