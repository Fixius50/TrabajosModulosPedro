{
    "name": "desarrollo-interfaces-root",
    "private": true,
    "scripts": {
        "install": "cd NovelaGraficaApp && npm install",
        "build": "cd NovelaGraficaApp && npm run build && cd ../.. && mkdir -p dist/NovelaGraficaApp && cp -r NovelaGraficaApp/dist/* dist/NovelaGraficaApp/ && cp index.html dist/",
        "dev": "cd NovelaGraficaApp && npm run dev"
    },
    "engines": {
        "node": ">=18"
    }
}