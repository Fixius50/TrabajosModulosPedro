{
    "name": "desarrollo-interfaces-root",
    "private": true,
    "scripts": {
        "install": "cd NovelaGraficaApp && npm install",
        "dev": "cd NovelaGraficaApp && npm run dev",
        "build": "cd NovelaGraficaApp && npm install && npm run build && cd .. && rm -rf dist && mkdir dist && cp -r NovelaGraficaApp/dist/* dist/"
    },
    "engines": {
        "node": ">=18"
    }
}