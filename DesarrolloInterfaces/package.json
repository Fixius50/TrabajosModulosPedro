{
    "name": "desarrollo-interfaces-root",
    "private": true,
    "scripts": {
        "install": "cd PersonalFinanceApp/index && npm install",
        "dev": "cd PersonalFinanceApp/index && npm run dev",
        "build": "rm -rf dist && mkdir dist && cp index.html dist/ && cd PersonalFinanceApp/index && npm install && npm run build && cd ../.. && mkdir -p dist/PersonalFinanceApp && cp -r PersonalFinanceApp/index/dist/* dist/PersonalFinanceApp/"
    },
    "engines": {
        "node": ">=18"
    }
}