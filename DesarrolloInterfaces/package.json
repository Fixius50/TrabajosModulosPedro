{
    "name": "desarrollo-interfaces-root",
    "private": true,
    "scripts": {
        "install": "cd PersonalFinanceApp && npm install",
        "dev": "cd PersonalFinanceApp && npm run dev",
        "build": "cd PersonalFinanceApp && npm install && npm run build && cd .. && rm -rf dist && mkdir dist && cp -r PersonalFinanceApp/dist/* dist/"
    },
    "engines": {
        "node": ">=18"
    }
}